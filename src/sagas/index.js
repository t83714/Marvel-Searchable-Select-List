import { takeEvery } from "redux-saga/effects";
import { APP_INIT } from "../actions/types";

// eslint-disable-next-line no-unused-vars
function* AppSaga(store) {
    //yield takeEvery(APP_INIT, loadOptions);
}

export default AppSaga;